import 'package:flutter/material.dart';

/// Non-IO stub of desktop_invite_section.dart, selected on platforms without
/// `dart:io` (web). See the conditional import in event_editor_screen.dart
/// and desktop_shell.dart.
bool get isLinuxDesktop => false;

Widget? desktopInviteSection(String eventId) => null;

Widget? desktopPendingInvitationsButton() => null;
