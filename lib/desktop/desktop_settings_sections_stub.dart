import 'package:flutter/material.dart';

/// Non-IO stub of desktop_settings_sections.dart, selected on platforms
/// without `dart:io` (web). See the conditional import in settings_screen.dart.
bool get isLinuxDesktop => false;

Widget? desktopAccountSection() => null;
Widget? desktopSyncSection() => null;
Widget? desktopRelaySection() => null;
