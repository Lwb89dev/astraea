<?xml version="1.0" encoding="utf-8"?>
<!-- The "add event" plus. A vector drawable is only safe in a widget from API
     24, where the launcher's RemoteViews inflation gained VectorDrawable
     support — that is exactly our minSdk, so this can't be relied on if minSdk
     is ever lowered. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FF161A2E">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M19,13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z" />
</vector>
